<div class="nested-fields">
	<%= f.input :title, collection: Product.all, label_method: :title, value_method: :id, label: "Title", include_blank: false %>
	<%= f.input :quantity %>
	

	<%= link_to_remove_association "Remove Product", f %>
</div>

<!-- <%= f.collection_select(:product_id, Product.all, :id, :title) %> -->
